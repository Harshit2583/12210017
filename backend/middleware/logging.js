// Replace this with your actual logging logic from the Pre-Test Setup
module.exports = (req, res, next) => {
  // Example: Log method and URL
  // You must replace this with your custom logger
  // e.g., log to a file, database, or external service
  // Do NOT use console.log in the final version
  next();
}; 